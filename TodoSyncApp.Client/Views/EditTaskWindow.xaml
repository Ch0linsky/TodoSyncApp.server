<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="av"
        x:Class="TodoSyncApp.Client.Views.EditTaskWindow"
        Title="Editar Tarea" Height="385" Width="531"
        WindowStartupLocation="CenterOwner"
        Background="#f8f9fa"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">

	<Window.Resources>
		<Storyboard x:Key="WindowLoadedAnimation">
			<DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3" />
			<DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" From="0.9" To="1" Duration="0:0:0.3" />
			<DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" From="0.9" To="1" Duration="0:0:0.3" />
		</Storyboard>
	</Window.Resources>

	<Border Margin="29,20,20,20" Padding="10" Background="White" CornerRadius="10" BorderBrush="#ccc" BorderThickness="1" RenderTransformOrigin="0.5,0.5">
		<Border.RenderTransform>
			<ScaleTransform ScaleX="1" ScaleY="1"/>
		</Border.RenderTransform>
		<StackPanel>
			<!-- Título -->
			<TextBlock Text="Título de la tarea:" FontWeight="Bold"/>
			<TextBox Text="{Binding Task.Title}" Height="30" Margin="0,5,0,10"/>

			<!-- Estado -->
			<StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
				<CheckBox IsChecked="{Binding Task.IsCompleted}" VerticalAlignment="Center"/>
				<TextBlock Text="Completada" VerticalAlignment="Center" Margin="5,0"/>
			</StackPanel>

			<!-- Prioridad -->
			<TextBlock Text="Prioridad:" FontWeight="Bold"/>
			<ComboBox SelectedValue="{Binding Task.Priority}" Height="30" Margin="0,5,0,10">
				<ComboBoxItem Content="Alta"/>
				<ComboBoxItem Content="Media"/>
				<ComboBoxItem Content="Baja"/>
			</ComboBox>

			<!-- Etiquetas -->
			<TextBlock Text="Etiquetas (separadas por coma):" FontWeight="Bold"/>
			<TextBox Text="{Binding Task.Tags}" Height="30" Margin="0,5,0,10"/>

			<!-- Fecha límite -->
			<TextBlock Text="Fecha límite:" FontWeight="Bold"/>
			<DatePicker SelectedDate="{Binding Task.DueDate}" Margin="0,5,0,10"/>

			<!-- Botones -->
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
				<Button Content="Guardar" Width="80" Margin="0,0,10,0" Padding="5,2" Command="{Binding SaveCommand}"/>
				<Button Content="Cancelar" Width="80" Padding="5,2" Click="OnCancelClick"/>
			</StackPanel>
		</StackPanel>
	</Border>
</Window>
